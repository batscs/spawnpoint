type project = {
    id: string,
    topics: string[],
    name: string,
    description: string,
    banner: string,
    isFavorite: boolean,
    isPublished: boolean,
    startDate: string,
    endDate: string,
    github: string,
    inspect: string,
}
